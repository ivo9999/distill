# Sample Data

Place Discord export JSON files here. Use DiscordChatExporter to export channels.

Expected format: array of message objects with `id`, `author.name`, `author.id`, `content`, `timestamp`, `reactions[]`, `reference.messageId`.
